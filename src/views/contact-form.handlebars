<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>Welcome Email</title>
    <style>
       body {
           font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
           background-color: #00d665;
           padding: 20px;
       }
       .container {
           max-width: 600px;
           margin: 0 auto;
           background-color: #ffffff;
           padding: 20px;
           border-radius: 50px;
           box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
       }
       .header {
           text-align: center;
           margin-bottom: 20px;
       }
       .header img {
           width: 100px;
           height: 100px;
           border-radius: 50%;
       }
       .attribute {
        background-color: #f5f5f5;
           margin-bottom: 25px;
           padding:10px;
       }
       .attribute label {
              font-weight: bold;
       }
       .attribute span {
           display: block;
           background-color: #ffffff;
           padding: 10px;
           margin-top: 5px;
       }

       .footer {
           text-align: center;
       }

       .footer h5 {
        font-weight: normal;
        font-size: 16px;
       }
       @media screen and (max-width: 600px) {
           .container {
               width: 100%;
               padding: 10px;
           }
           .header img {
               width: 70px;
               height: 70px;
           }
       }
   </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="https://zorgeloosbesparen.nl/assets/img/Logo%20parts/ZorgeloosBesparen_logo_original_transparant.png" alt="Logo" width="64">
        </div>
        <div class="attribute">
            <label>Name:</label>
            <span>{{name}}</span>
        </div>
        <div class="attribute">
            <label>Email:</label>
            <span>{{email}}</span>
        </div>
        <div class="attribute">
            <label>Subject:</label>
            <span>{{subject}}</span>
        </div>
        <div class="attribute">
            <label>Message:</label>
            <span>{{message}}</span>
        </div>
        <div class="footer"><h5>&copy; 2024 Zorgeloos Besparen. All rights reserved.</h5></div>
    </div>
</body>
</html>
        
</body>
</html>